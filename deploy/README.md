# deploy/

Versioned artifacts that are **not** loaded from this repo and must be copied
into a location Claude Code reads before they take effect. Kept here so they
travel with the source and stay under version control.

- `agents/` — predefined subagent definitions for the
  `subagent-driven-development` skill (`sdd-high`, `sdd-escalate`). Install per
  `agents/README.md`.
