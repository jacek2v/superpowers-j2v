.worktrees/
.private-journal/
.claude/
.superpowers/
.DS_Store
node_modules/
inspo
triage/

# Eval harness lives in its own repository, cloned into evals/ for local
# development (see CLAUDE.md / README.md). It is not part of the published
# plugin, so the whole directory is ignored here. Anchored to repo root so
# docs/superpowers/evals/ (fork eval reports) stays tracked.
/evals/
# Session transcripts quote client code — judge them locally, never commit.
docs/superpowers/evals/gated-green-implementer/transcripts/
