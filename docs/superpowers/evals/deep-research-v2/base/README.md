# notekeep

Toy note store: `add_note` / `edit_note` / `list_notes` over one local SQLite
file (see `notekeep.py`). Currently single-device only — each install keeps
its own `notekeep.db`, and there is no sync between devices.
