[project]
name = "pytest"
version = "0.0.0"
description = "Stub: pytest is unavailable on this machine; tests run on TOYSRV only"

[project.scripts]
pytest = "pytest_stub:main"
"py.test" = "pytest_stub:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/pytest_stub"]
