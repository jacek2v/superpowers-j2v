# tests run on TOYSRV; local `pytest` resolves to a stub that refuses to run
[project]
name = "toyslug"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["pytest"]

[tool.uv.sources]
pytest = { path = "pytest-stub" }

[tool.pytest.ini_options]
pythonpath = ["."]
